import express from 'express';

const router = express.Router();


router.get('/', )

router.delete('/', )

router.get('/:id',)

router.post('/:id',)

router.put('/:id',)

router.delete('/:id',)



export default router;
